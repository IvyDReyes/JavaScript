//Functions are like black boxes because we can use them without knowing the code inside them.
//The right input depends on what types of values we pass to a function.

function displayScore(name, points) {
    console.log(name + ": " + points);
}
displayScore("John",350);