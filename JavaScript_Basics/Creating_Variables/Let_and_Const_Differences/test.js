//We use const to declare variables whose values are not suppose to change. const is short for "constant". 
const norwayCapital = "Oslo";

//We can update the let variables with the assign = operator
let age = 90;
age = 91;